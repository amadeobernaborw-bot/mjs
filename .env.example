# MJ STORE — Plantilla de variables de entorno
# Copiar este archivo a `.env.local` y reemplazar los valores con los reales.
# `.env.local` NUNCA debe subirse al repositorio (está en .gitignore).

# Supabase — obtener en https://supabase.com/dashboard/project/<id>/settings/api
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...REPLACE_ME
